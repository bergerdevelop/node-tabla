# Notas
```

Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base numerica de la tabla de multiplicar
                                                            [número] [requerido]
  -l, --listar   Permite listar la tabla generada                     [booleano]
  -h, --hasta    Permite definir hasta donde va la tabla[número] [defecto: "10"]

  ```